import React from 'react'

const sendMessage = async (message, image = null) => {

    const format = {
        
    }

    return (
        <div>
            
        </div>
    )
}

export default sendMessage